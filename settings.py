import os


UHHH = os.getenv(
    "MYSTERY",
    "1ST HALF-TOKEN"
    "2ND HALF-TOKEN",
)
FIRST = os.getenv(
    "TOKEN 1",
    "1ST HALF-TOKEN"
    "2ND HALF-TOKEN",
)
SECOND = os.getenv(
    "TOKEN 1",
    "1ST HALF-TOKEN"
    "2ND HALF-TOKEN",
)
THIRD = os.getenv(
    "TOKEN 3",
    "1ST HALF-TOKEN"
    "2ND HALF-TOKEN",
)


API_URL = os.getenv("API_URL", "URL HERE")


COMMAND_PREFIX = "$"
